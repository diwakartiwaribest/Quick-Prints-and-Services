const translations = {
    en: {
        "nav-home": "Home",
        "nav-about": "About",
        "nav-services": "Services",
        "nav-why": "Why Us",
        "nav-contact": "Contact",
        "hero-title": "Quick Prints and Services",
        "hero-subtitle": "Printing, Online Services & Daily Essentials in One Place",
        "btn-whatsapp": "Chat on WhatsApp",
        "btn-services": "View Services",
        "about-title": "About Us",
        "about-text": "Welcome to <strong>Quick Prints and Services</strong>. We are a professional, all-in-one local shop dedicated to making your life easier. Whether you need high-quality printing, photocopy, assistance with online document work and ticket booking, or just want to grab daily grocery essentials, we've got you covered. Fast, reliable, and affordable services tailored for our community.",
        "services-title": "Our Services",
        "svc-printing-title": "Printing",
        "svc-printing-desc": "High-quality black & white and color document printing.",
        "svc-copy-title": "Photocopy",
        "svc-copy-desc": "Fast and crisp photocopying services for all document sizes.",
        "svc-form-title": "Online Form Filling",
        "svc-form-desc": "Assistance with job applications, government forms, and more.",
        "svc-ticket-title": "Ticket Booking",
        "svc-ticket-desc": "Train, flight, and bus ticket booking made easy.",
        "svc-recharge-title": "Recharge & Bills",
        "svc-recharge-desc": "Mobile recharges and utility bill payments.",
        "svc-grocery-title": "Grocery Items",
        "svc-grocery-desc": "Fresh and packaged daily grocery essentials.",
        "svc-store-title": "General Store",
        "svc-store-desc": "Stationery, snacks, and daily household products.",
        "svc-project-title": "Project Work Help",
        "svc-project-desc": "Topic research, diagram search, project formatting & cover design.",
        "svc-digital-title": "Digital Assistance",
        "svc-digital-desc": "DigiLocker setup, email submissions, and document upload help.",
        "svc-software-title": "Software Installation & Fixes",
        "svc-software-desc": "Assistance with software setup, driver updates, and resolving errors.",
        "why-title": "Why Choose Us",
        "why-fast-title": "Fast Service",
        "why-fast-desc": "We value your time and deliver prompt results.",
        "why-price-title": "Affordable Price",
        "why-price-desc": "Best quality services at pocket-friendly rates.",
        "why-trust-title": "Trusted Shop",
        "why-trust-desc": "Reliable and secure handling of your documents.",
        "why-all-title": "All-in-One",
        "why-all-desc": "Multiple services under a single roof.",
        "contact-title": "Contact Us",
        "contact-msg-title": "We are just a message away!",
        "contact-msg-desc": "For any inquiries, services, or support, feel free to reach out to us on WhatsApp.",
        "btn-msg": "Message on WhatsApp",
        "btn-call": "Call Now",
        "contact-visit-title": "Visit Our Shop",
        "contact-visit-desc": "Kunwar Singh Chauraha, near GGIC School, Officers Awaas, Ballia, Uttar Pradesh 277001",
        "btn-dir": "Get Directions",
        "footer-desc": "Printing, Online Services & Daily Essentials",
        "footer-rights": "Quick Prints and Services. All rights reserved."
    },
    hi: {
        "nav-home": "होम",
        "nav-about": "हमारे बारे में",
        "nav-services": "सेवाएं",
        "nav-why": "हम ही क्यों",
        "nav-contact": "संपर्क",
        "hero-title": "Quick Prints and Services",
        "hero-subtitle": "प्रिंटिंग, ऑनलाइन सेवाएं और दैनिक जरूरत का सामान एक ही जगह",
        "btn-whatsapp": "WhatsApp पर बात करें",
        "btn-services": "सेवाएं देखें",
        "about-title": "हमारे बारे में",
        "about-text": "<strong>Quick Prints and Services</strong> में आपका स्वागत है। हम एक पेशेवर, ऑल-इन-वन लोकल शॉप हैं जो आपके जीवन को आसान बनाने के लिए समर्पित हैं। चाहे आपको उच्च गुणवत्ता वाली प्रिंटिंग, फोटोकॉपी, ऑनलाइन फॉर्म भरने या टिकट बुकिंग में सहायता की आवश्यकता हो, या बस दैनिक किराने का सामान चाहिए, हम आपके लिए उपलब्ध हैं। हमारे समुदाय के लिए तेज़, विश्वसनीय और किफायती सेवाएं।",
        "services-title": "हमारी सेवाएं",
        "svc-printing-title": "प्रिंटिंग",
        "svc-printing-desc": "उच्च गुणवत्ता वाली ब्लैक एंड व्हाइट और रंगीन दस्तावेज़ प्रिंटिंग।",
        "svc-copy-title": "फोटोकॉपी",
        "svc-copy-desc": "सभी प्रकार के दस्तावेज़ों के लिए तेज़ और स्पष्ट फोटोकॉपी सेवाएं।",
        "svc-form-title": "ऑनलाइन फॉर्म भरना",
        "svc-form-desc": "नौकरी के आवेदन, सरकारी फॉर्म आदि में सहायता।",
        "svc-ticket-title": "टिकट बुकिंग",
        "svc-ticket-desc": "ट्रेन, फ्लाइट और बस टिकट बुकिंग हुई आसान।",
        "svc-recharge-title": "रिचार्ज और बिल",
        "svc-recharge-desc": "मोबाइल रिचार्ज और उपयोगिता बिल भुगतान।",
        "svc-grocery-title": "किराने का सामान",
        "svc-grocery-desc": "ताज़ा और पैक किया हुआ दैनिक किराने का सामान।",
        "svc-store-title": "जनरल स्टोर",
        "svc-store-desc": "स्टेशनरी, स्नैक्स और रोजमर्रा के घरेलू उत्पाद।",
        "svc-project-title": "प्रोजेक्ट कार्य सहायता",
        "svc-project-desc": "विषय अनुसंधान, आरेख खोज, प्रोजेक्ट फॉर्मेटिंग और कवर डिज़ाइन।",
        "svc-digital-title": "डिजिटल सहायता",
        "svc-digital-desc": "डिजिलॉकर सेटअप, ईमेल सबमिशन और दस्तावेज़ अपलोड सहायता।",
        "svc-software-title": "सॉफ्टवेयर इंस्टॉलेशन और सुधार",
        "svc-software-desc": "सॉफ्टवेयर सेटअप, ड्राइवर अपडेट और त्रुटियों को हल करने में सहायता।",
        "why-title": "हमें क्यों चुनें",
        "why-fast-title": "तेज़ सेवा",
        "why-fast-desc": "हम आपके समय को महत्व देते हैं और तुरंत परिणाम देते हैं।",
        "why-price-title": "किफायती कीमत",
        "why-price-desc": "जेब के अनुकूल दरों पर सर्वोत्तम गुणवत्ता वाली सेवाएं।",
        "why-trust-title": "भरोसेमंद दुकान",
        "why-trust-desc": "आपके दस्तावेज़ों का सुरक्षित संचालन।",
        "why-all-title": "ऑल-इन-वन",
        "why-all-desc": "एक ही छत के नीचे कई सेवाएं।",
        "contact-title": "संपर्क करें",
        "contact-msg-title": "हम बस एक मैसेज दूर हैं!",
        "contact-msg-desc": "किसी भी पूछताछ, सेवा या सहायता के लिए, कृपया बेझिझक हमसे WhatsApp पर संपर्क करें।",
        "btn-msg": "WhatsApp पर मैसेज करें",
        "btn-call": "अभी कॉल करें",
        "contact-visit-title": "हमारी दुकान पर आएं",
        "contact-visit-desc": "कुंवर सिंह चौराहा, जीजीआईसी स्कूल के पास, ऑफिसर्स आवास, बलिया, उत्तर प्रदेश 277001",
        "btn-dir": "रास्ता देखें",
        "footer-desc": "प्रिंटिंग, ऑनलाइन सेवाएं और दैनिक जरूरत का सामान",
        "footer-rights": "Quick Prints and Services. सर्वाधिकार सुरक्षित।"
    }
};

let currentLang = localStorage.getItem('lang') || 'en';

function setLanguage(lang) {
    currentLang = lang;
    localStorage.setItem('lang', lang);

    // Update language toggle button text
    const langBtnText = document.getElementById('lang-btn-text');
    if (langBtnText) {
        langBtnText.textContent = lang === 'en' ? 'हिं' : 'EN';
    }

    document.querySelectorAll('[data-i18n]').forEach(element => {
        const key = element.getAttribute('data-i18n');
        if (translations[lang] && translations[lang][key]) {
            if (element.tagName === 'P' || element.tagName === 'SPAN' || element.tagName === 'H1' || element.tagName === 'H2' || element.tagName === 'H3' || element.tagName === 'H4' || element.tagName === 'DIV' || element.tagName === 'A') {
                // If it contains icons, we need to preserve them. 
                // A simple way is to replace only text nodes or wrap text in spans.
                // We'll update innerHTML since about-text contains <strong>
                element.innerHTML = translations[lang][key];
            }
        }
    });
}

document.addEventListener('DOMContentLoaded', () => {

    // Initialize Language
    setLanguage(currentLang);
    const langToggle = document.getElementById('lang-toggle');
    if (langToggle) {
        langToggle.addEventListener('click', (e) => {
            e.preventDefault();
            const newLang = currentLang === 'en' ? 'hi' : 'en';
            setLanguage(newLang);
        });
    }

    // Set Current Year in Footer
    const yearSpan = document.getElementById('year');
    if (yearSpan) {
        yearSpan.textContent = new Date().getFullYear();
    }

    // Loader Remove
    window.addEventListener('load', () => {
        const loader = document.getElementById('loader');
        if (loader) {
            loader.style.opacity = '0';
            setTimeout(() => { loader.style.display = 'none'; }, 500);
        }
    });

    // Sticky Navbar (desktop only) & Back to Top Button
    const navbar = document.getElementById('navbar');
    const backToTopBtn = document.getElementById('backToTop');

    window.addEventListener('scroll', () => {
        // Sticky navbar style on desktop
        if (navbar) {
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        }
        // Back-to-top visibility
        if (window.scrollY > 400) {
            backToTopBtn.classList.add('show');
        } else {
            backToTopBtn.classList.remove('show');
        }
    });

    // Back to Top
    if (backToTopBtn) {
        backToTopBtn.addEventListener('click', () => {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        });
    }

    // ---------------------------------------------------
    // Active link highlighting on scroll
    // Works for both desktop (.nav-link) and mobile (.mob-nav-link)
    // ---------------------------------------------------
    const sections = document.querySelectorAll('section[id]');
    const desktopLinks = document.querySelectorAll('.nav-link');
    const mobileLinks = document.querySelectorAll('.mob-nav-link');

    function updateActiveLinks() {
        let current = '';
        sections.forEach(section => {
            const sectionTop = section.offsetTop - 120;
            if (window.scrollY >= sectionTop) {
                current = section.getAttribute('id');
            }
        });

        // Desktop
        desktopLinks.forEach(link => {
            link.classList.remove('active');
            const href = link.getAttribute('href');
            if (href && href.includes(current)) {
                link.classList.add('active');
            }
        });

        // Mobile bottom bar
        mobileLinks.forEach(link => {
            link.classList.remove('active');
            const href = link.getAttribute('href');
            if (href && href.includes(current)) {
                link.classList.add('active');
            }
        });
    }

    window.addEventListener('scroll', updateActiveLinks, { passive: true });
    updateActiveLinks(); // run once on load

    // Smooth-scroll for mobile bottom nav links + bounce animation
    mobileLinks.forEach(link => {
        link.addEventListener('click', (e) => {
            const href = link.getAttribute('href');
            if (href && href.startsWith('#')) {
                e.preventDefault();
                // Bounce the icon
                link.classList.add('bounce');
                link.addEventListener('animationend', () => link.classList.remove('bounce'), { once: true });
                const target = document.querySelector(href);
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            }
        });
    });

    // ---------------------------------------------------
    // Fade-in + pop-in Animation Observer
    // Targets: .fade-in-up, .service-card, .why-card, .title-underline
    // ---------------------------------------------------
    const observeTargets = document.querySelectorAll(
        '.fade-in-up, .service-card, .why-card, .title-underline'
    );
    const appearOptions = { threshold: 0.12, rootMargin: '0px 0px -40px 0px' };

    const appearOnScroll = new IntersectionObserver((entries, observer) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('visible');
                observer.unobserve(entry.target);
            }
        });
    }, appearOptions);

    observeTargets.forEach(el => appearOnScroll.observe(el));

    // ---------------------------------------------------
    // Hero Carousel Logic (Infinite loop & Dots)
    // ---------------------------------------------------
    const carousel = document.getElementById('heroCarousel');
    const dotsContainer = document.getElementById('carouselDots');
    if (carousel && dotsContainer) {
        const originalImages = Array.from(carousel.querySelectorAll('.carousel-img'));
        let currentIndex = 0;
        let autoScrollInterval;
        let isTransitioning = false;

        // Clone the first image to append at the end for seamless infinite scroll
        const firstClone = originalImages[0].cloneNode(true);
        carousel.appendChild(firstClone);

        // Create pagination dots dynamically (only for original images)
        originalImages.forEach((_, index) => {
            const dot = document.createElement('div');
            dot.classList.add('carousel-dot');
            if (index === 0) dot.classList.add('active');
            dot.addEventListener('click', () => {
                scrollToImage(index);
                resetInterval();
            });
            dotsContainer.appendChild(dot);
        });

        const dots = dotsContainer.querySelectorAll('.carousel-dot');

        function updateDots(index) {
            dots.forEach(d => d.classList.remove('active'));
            // If at the clone (last item), highlight the first dot
            const activeIndex = index >= originalImages.length ? 0 : index;
            if (dots[activeIndex]) dots[activeIndex].classList.add('active');
        }

        function scrollToImage(index, smooth = true) {
            currentIndex = index;
            const scrollLeft = carousel.clientWidth * index;

            if (!smooth) {
                carousel.style.scrollBehavior = 'auto';
                carousel.style.scrollSnapType = 'none'; // Temporarily disable snap to avoid glitch
            }

            carousel.scrollTo({
                left: scrollLeft,
                behavior: smooth ? 'smooth' : 'auto'
            });

            if (!smooth) {
                requestAnimationFrame(() => {
                    requestAnimationFrame(() => {
                        carousel.style.scrollBehavior = '';
                        carousel.style.scrollSnapType = '';
                    });
                });
            }

            updateDots(index);
        }

        function startAutoScroll() {
            autoScrollInterval = setInterval(() => {
                if (isTransitioning) return;

                currentIndex++;
                scrollToImage(currentIndex, true);

                // If we reached the cloned image, silently snap back to the actual first image
                if (currentIndex >= originalImages.length) {
                    isTransitioning = true;
                    setTimeout(() => {
                        scrollToImage(0, false);
                        isTransitioning = false;
                    }, 600); // 600ms allows the smooth scroll to finish first
                }
            }, 10000); // 10 seconds per slide
        }

        function resetInterval() {
            clearInterval(autoScrollInterval);
            startAutoScroll();
        }

        startAutoScroll();

        // Update dots if user manually scrolls the carousel
        let scrollTimeout;
        carousel.addEventListener('scroll', () => {
            if (isTransitioning) return;

            const scrollPosition = carousel.scrollLeft;
            const index = Math.round(scrollPosition / carousel.clientWidth);

            if (currentIndex !== index) {
                currentIndex = index;
                updateDots(index);

                // If user manually scrolls to the clone, snap back to 0
                if (currentIndex >= originalImages.length) {
                    clearTimeout(scrollTimeout);
                    scrollTimeout = setTimeout(() => {
                        isTransitioning = true;
                        scrollToImage(0, false);
                        setTimeout(() => isTransitioning = false, 50);
                    }, 150);
                }
            }
        });
    }

    // ---------------------------------------------------
    // Hero Background Animation (Premium Motif)
    // ---------------------------------------------------
    const heroCanvas = document.getElementById('hero-canvas');
    if (heroCanvas) {
        const hctx = heroCanvas.getContext('2d');
        let HW, HH;

        function hResize() {
            const hr = heroCanvas.parentElement.getBoundingClientRect();
            HW = heroCanvas.width = hr.width;
            HH = heroCanvas.height = hr.height;
        }

        // ── 1. FLOATING HEXAGONS
        class Hex {
            constructor(init) { this.reset(init); }
            reset(init) {
                this.x = Math.random() * HW;
                this.y = init ? Math.random() * HH : HH + 50;
                this.size = 14 + Math.random() * 22;
                this.vx = (Math.random() - .5) * .25;
                this.vy = -(0.4 + Math.random() * .45);
                this.rot = Math.random() * Math.PI;
                this.vrot = (Math.random() - .5) * .008;
                this.alpha = 0;
                this.targetA = .13 + Math.random() * .15;
                this.life = 0;
                this.maxL = 260 + Math.random() * 180;
                const blues = ['21,101,192', '25,118,210', '66,165,245', '13,71,161'];
                this.rgb = blues[Math.floor(Math.random() * blues.length)];
                this.filled = Math.random() > .5;
            }
            hexPath(cx, cy, r) {
                hctx.beginPath();
                for (let i = 0; i < 6; i++) {
                    const a = (Math.PI / 3) * i - Math.PI / 6;
                    i === 0 ? hctx.moveTo(cx + r * Math.cos(a), cy + r * Math.sin(a))
                        : hctx.lineTo(cx + r * Math.cos(a), cy + r * Math.sin(a));
                }
                hctx.closePath();
            }
            update() {
                this.x += this.vx; 
                this.y += this.vy; 
                this.rot += this.vrot;
                
                // Fade in at bottom, fade out at top
                const prog = 1 - (this.y / HH); // 0 at bottom, 1 at top
                if (prog < 0.15) {
                    this.alpha = this.targetA * (prog / 0.15);
                } else if (prog > 0.85) {
                    this.alpha = this.targetA * (1 - (prog - 0.85) / 0.15);
                } else {
                    this.alpha = this.targetA;
                }

                if (this.y < -this.size * 2) this.reset(false);
            }
            draw() {
                hctx.save(); hctx.translate(this.x, this.y); hctx.rotate(this.rot);
                this.hexPath(0, 0, this.size);
                if (this.filled) { hctx.fillStyle = `rgba(${this.rgb},${this.alpha * .4})`; hctx.fill(); }
                hctx.strokeStyle = `rgba(${this.rgb},${this.alpha})`; hctx.lineWidth = 1.5; hctx.stroke();
                hctx.restore();
            }
        }

        // ── 2. RISING DOTS
        class Dot {
            constructor(init) { this.reset(init); }
            reset(init) {
                this.x = Math.random() * HW;
                this.y = init ? Math.random() * HH : HH + 10;
                this.r = 2.5 + Math.random() * 4;
                this.vy = -(0.4 + Math.random() * .5);
                this.vx = (Math.random() - .5) * .2;
                this.alpha = 0;
                this.maxA = .18 + Math.random() * .2;
                this.life = 0;
                this.maxL = 180 + Math.random() * 120;
                const stops = ['21,101,192', '25,118,210', '66,165,245', '144,202,249'];
                this.rgb = stops[Math.floor(Math.random() * stops.length)];
            }
            update() {
                this.x += this.vx + Math.sin(hFrame * .05) * .15;
                this.y += this.vy;
                
                const prog = 1 - (this.y / HH);
                if (prog < 0.1) {
                    this.alpha = this.maxA * (prog / 0.1);
                } else if (prog > 0.9) {
                    this.alpha = this.maxA * (1 - (prog - 0.9) / 0.1);
                } else {
                    this.alpha = this.maxA;
                }

                if (this.y < -this.r * 2) this.reset(false);
            }
            draw() {
                hctx.beginPath(); hctx.arc(this.x, this.y, this.r, 0, Math.PI * 2);
                hctx.fillStyle = `rgba(${this.rgb},${this.alpha})`; hctx.fill();
            }
        }

        // ── 3. SCAN LINES
        class ScanLine {
            constructor(delay) {
                this.y = -20; this.speed = .8 + Math.random() * .6; this.delay = delay;
                this.timer = 0; this.active = false; this.alpha = 0;
            }
            update() {
                this.timer++; if (this.timer < this.delay) return;
                this.active = true; this.y += this.speed;
                const prog = this.y / HH;
                this.alpha = prog < .1 ? prog / .1 : prog > .85 ? (1 - prog) / .15 : 1;
                if (this.y > HH + 20) { this.y = -20; this.timer = 0; this.delay = 180 + Math.floor(Math.random() * 200); this.active = false; }
            }
            draw() {
                if (!this.active) return;
                const a = this.alpha * .18;
                const grad = hctx.createLinearGradient(0, this.y - 6, 0, this.y + 6);
                grad.addColorStop(0, `rgba(66,165,245,0)`); grad.addColorStop(.5, `rgba(66,165,245,${a})`); grad.addColorStop(1, `rgba(66,165,245,0)`);
                hctx.fillStyle = grad; hctx.fillRect(0, this.y - 6, HW, 12);
                hctx.beginPath(); hctx.moveTo(0, this.y); hctx.lineTo(HW, this.y);
                hctx.strokeStyle = `rgba(100,181,246,${this.alpha * .55})`; hctx.lineWidth = 1; hctx.stroke();
            }
        }

        let gridCircles = [];
        function buildGrid() {
            gridCircles = []; const gx = 44, gy = 44; let idx = 0;
            for (let x = gx / 2; x < HW + gx; x += gx) {
                for (let y = gy / 2; y < HH + gy; y += gy) {
                    gridCircles.push({ x, y, phase: idx * .55 }); idx++;
                }
            }
        }
        function drawGrid(t) {
            gridCircles.forEach(c => {
                const wave = Math.sin(t * .9 + c.phase) * .5 + .5;
                const a = .07 + wave * .13, r = 1.4 + wave * 1.2;
                hctx.beginPath(); hctx.arc(c.x, c.y, r, 0, Math.PI * 2);
                hctx.fillStyle = `rgba(21,101,192,${a})`; hctx.fill();
            });
        }
        function drawBlobs(t) {
            const g1 = hctx.createRadialGradient(HW * .85, HH * .18, 0, HW * .85, HH * .18, HW * .5);
            g1.addColorStop(0, `rgba(66,165,245,0.22)`); g1.addColorStop(.55, `rgba(100,181,246,0.08)`); g1.addColorStop(1, `rgba(255,255,255,0)`);
            hctx.fillStyle = g1; hctx.fillRect(0, 0, HW, HH);
            const ox = Math.sin(t * .25) * 12, oy = Math.cos(t * .3) * 8;
            const g2 = hctx.createRadialGradient(HW * .12 + ox, HH * .88 + oy, 0, HW * .12 + ox, HH * .88 + oy, HW * .42);
            g2.addColorStop(0, `rgba(21,101,192,0.16)`); g2.addColorStop(.6, `rgba(66,165,245,0.06)`); g2.addColorStop(1, `rgba(255,255,255,0)`);
            hctx.fillStyle = g2; hctx.fillRect(0, 0, HW, HH);
        }

        hResize(); buildGrid();
        const hexes = Array.from({ length: 14 }, (_, i) => new Hex(true));
        const dots = Array.from({ length: 20 }, () => new Dot(true));
        const scans = [new ScanLine(80)];
        let hFrame = 0;
        function hLoop() {
            hctx.clearRect(0, 0, HW, HH); const t = hFrame++ * .016;
            drawBlobs(t); drawGrid(t);
            scans.forEach(s => { s.update(); s.draw(); });
            dots.forEach(d => { d.update(); d.draw(); });
            hexes.forEach(h => { h.update(); h.draw(); });
            requestAnimationFrame(hLoop);
        }
        hLoop();
        window.addEventListener('resize', () => { hResize(); buildGrid(); });
    }

});

